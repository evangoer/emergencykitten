<div class="centered">
    {{#kitten}}
        <p>
            <a href="{{url}}" title="{{title}}">
                <img class="kid-image" src="/img/{{id}}" alt="{{imageAlt}}">
            </a>
        </p>

        <p class="permalink">
            Like this picture? Here's a <a href="/pic/{{id}}" rel="bookmark">permalink</a>!

            <br>
            <input id="permalink-input" readonly
                value="http://www.emergencygeorge.com/pic/{{id}}">
        </p>
    {{/kitten}}
</div>

<script>
(function (win, doc) {
    doc.getElementById('permalink-input').addEventListener('focus', function () {
        var el = this;

        setTimeout(function () {
            el.select();
        }, 1);
    }, false);
}(this, document));
</script>
